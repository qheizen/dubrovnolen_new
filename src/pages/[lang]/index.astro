---
import Layout from '../../layouts/baseLayout.astro';
import { languages } from '../../i18n/utils';

export function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({
    params: { lang },
  }));
}

const { lang } = Astro.params;
---
<Layout titleKey = 'index' lang = {lang}>
</Layout>
